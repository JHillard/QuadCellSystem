function inoise = opamp_vnoise(dc, pole, slope); 
%This function calculates the current noise of an opamp based off a
%single pole model. Assumes all input values are linear.
    P = log10(p);
    F = log10(f);
    %P = log10(peak);
    %T = log10(trough);
        
    n = -(P-T)/CORN_F*F + P;
    n = 10.^n
    n(f>corner_freq) = trough;
    vnoise = n;
    
    
end
